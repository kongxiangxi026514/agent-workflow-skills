{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "agent": {
    "build":  { "mode": "primary",  "description": "Configured implementation model for build work. Use the routing policy to select Sol for qualifying complex deep-reasoning design or diagnosis.", "model": "REPLACE_WITH_IMPL_MODEL_ID" },
    "review": { "mode": "subagent", "description": "Cross-model reviewer/verifier; MUST be a different family than both implementation and deep-reasoning models.", "model": "REPLACE_WITH_REVIEW_MODEL_ID", "permission": { "edit": "deny" } }
  }
}
